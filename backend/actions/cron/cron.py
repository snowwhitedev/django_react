from backend.models import CronLog

def test():
    # cronLog = CronLog()
    # cronLog.description = "test cron"
    # cronLog.save()
    # # UserHandbook.objects.get(id=2).delete()
    print("hello")